---
import LinkButton from "./LinkButton.astro";
import { IDENTITY } from "../constants.js";
---

<footer class="footer">
  {
    Object.entries(IDENTITY.socialLinks).map(([platformName, endpointUrl]) => (
      <LinkButton name={platformName} url={endpointUrl} />
    ))
  }
</footer>
